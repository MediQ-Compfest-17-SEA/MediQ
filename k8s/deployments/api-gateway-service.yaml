apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: mediq
  labels:
    app: api-gateway
spec:
  type: ClusterIP
  ports:
  - port: 8601
    targetPort: 8601
    protocol: TCP
    name: http
  selector:
    app: api-gateway
